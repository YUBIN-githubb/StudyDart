void main() {

  // collection if
  // if giveMeFive is ture then add 5
  var giveMeFive = true;
  var numbers = [1, 2, 3, if (giveMeFive) 5,];
  numbers.add(10);
  numbers.first;
  numbers.last;
  print(numbers);
}